import math

x = float(input("x = "))
a = float(input("a = "))
b = float(input("b = "))
c = float(input("c = "))

f = (math.pow(2, x) + 4)

X = math.sqrt(a * math.cos (b) + math.pow (math.e, c)) + math.log (math.fabs(f), 7)

print((X))
