import math
a=float(input("Введіть число a:"))
b=float(input("Введіть число b:"))
h=float(input("Введіть число h:"))
spisok=[]
x=a
while x<b:
    f=x / math.pow(2, x) + math.pow(math.e, x)
    spisok.append(f)
    x=x+h
print(spisok)
